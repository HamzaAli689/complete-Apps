import 'package:flutter/material.dart';

class NextScreens extends StatelessWidget {
  const NextScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
        child: Container(
          child: Text("Malik Sab"),
        ),
      ),
    );
  }
}
