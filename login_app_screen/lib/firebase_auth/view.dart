import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'logic.dart';

class Firebase_authPage extends StatelessWidget {
  Firebase_authPage({Key? key}) : super(key: key);

  final logic = Get.put(Firebase_authLogic());

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
